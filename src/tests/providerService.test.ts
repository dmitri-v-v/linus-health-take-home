import { getProvidersByLocation } from '../services/providerService';

describe('getProvidersByLocation', () => {
	it('should return providers at a valid location', async () => {
	});

	it('should return empty list for a non-existent location', async () => {
	});
});
